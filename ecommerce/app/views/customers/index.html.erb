<p id="notice"><%= notice %></p>
<h1>Customers</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>City</th>
      <th>Pincode</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.name %></td>
        <td><%= customer.email %></td>
        <td><%= customer.city %></td>
        <td><%= customer.pincode %></td>
        <td><%= link_to 'Show', customer %></td>
        <td><%= link_to 'Show Order', orders_customer_path(customer) %></td>
        <td><%= link_to 'Edit', edit_customer_path(customer) %></td>
        <td><%= link_to 'Destroy', customer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Customer', new_customer_path %>
<br>
<p><%= link_to 'Order List', orders_path %></p>
<p><%= link_to 'Items List', items_path %></p>
