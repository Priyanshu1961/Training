<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>
      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div >
  <div style="float:left;">
    <%= f.label :customer_name %>
    <%= f.collection_select(:customer_name, Customer.all, :name, :name ) %>
  </div>
  <div style="margin-left:20%">
    <%= f.label :item_name %>
    <%= f.collection_select(:item_name, Item.all, :name, :name )  %>
  </div>
  <br><br><br>
</div >
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
